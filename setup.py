from setuptools import setup, find_packages

setup(
    name='calculadora',
    description='Tutorial de teste unitário em Python',
    packages=find_packages(),
    install_requires=[],
    test_suite='tests',
)
